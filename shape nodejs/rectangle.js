// rectangle.js
module.exports = {
    calcArea: function(length, breadth) {
      return length * breadth;
    },
    calcPerimeter: function(length, breadth) {
      return 2 * (length + breadth);
    }
  };
  